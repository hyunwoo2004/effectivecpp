#include<iostream>
#include <list>

// Effective C++
// 1. C++에 왔으면 C++ 법을 따릅시다.
// 항목 4: 객체를 사용하기 전에 반드시 그 객체를 초기화하자

// 기본제공 타입의 멤버를 초기화 리스트로 넣는 일이 선택이 아니라 의무인 경우
// -> 상수이거나 참조자로 되어있는 데이터 멤버의 경우(반드시 초기화되어야 함)
// 상수와 참조자는 대입 자체가 불가능함
// -> 어떨 떄는 꼭 초기화해야 하고 어떨 떄는 안해도 되고를 일일이 아는 것보다
//    항상 초기화 리스트를 사용하는 것이 더 편할 것

int main() {

} 

