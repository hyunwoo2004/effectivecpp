#include<iostream>

// Effective C++
// 1. C++에 왔으면 C++ 법을 따릅시다.
// 항목 3: 낌새만 보이면 const를 들이대 보자!

// const키워드는 생긴 것 답지 않게 그야말로 팔방미인!
// 클래스 바깥에서는 전역 혹은 네임스페이스 유호범위의 상수를 선언(정의)하는데 쓸 수 있다.
// 파일, 함수, 블록 유효범위에서 static으로 건언한 객체에도 const를 붙일 수 있다.
// 클래스 내부의 경우는 정적 멤버 및 비정적 데이터 멤버 모두를 상수롤 선언할 수 있다.

int main() {}
