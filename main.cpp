#include<iostream>

// Effective C++
// 1. C++에 왔으면 C++ 법을 따릅시다.
// 항목 1: C++ 언어를 연합체로 바라보는 안목은 필수

// C++ 구성
// STL
// Standard Template Language의 약자로, 말 그대로 템플릿 라이브러리이다.
// 컨테이너(container), 반복자(iterator), 알고리즘(algorithm), 함수 객체(function object)들이 서로 엄청 얽혀있도록
// 돌아가는 것이 규약, 그러나 템플릿과 라이브러리는 얼마든지 다른 아이디어를 중심으로 만들어질 수 있음
// 또한 STL만의 사용 규칙이 있으니 사용할 때 규칙만 잘 지키자!

int main() {
  
}
